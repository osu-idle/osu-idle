{
  "name": "@osu-idle/tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --exclude src/scenarios.test.ts --exclude src/benchmark.test.ts",
    "test:watch": "vitest --exclude src/scenarios.test.ts --exclude src/benchmark.test.ts",
    "testplay": "vitest run src/scenarios.test.ts",
    "testplay:watch": "vitest src/scenarios.test.ts",
    "benchmark": "vitest run src/benchmark.test.ts",
    "benchmark:watch": "vitest src/benchmark.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@osu-idle/shared": "*",
    "@osu-idle/server": "*",
    "@osu-idle/client": "*",
    "@osu-idle/web": "*",
    "osu-parsers": "^4.1.7"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
